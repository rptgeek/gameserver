{
  "name": "7d2d-infra",
  "version": "0.1.0",
  "private": true,
  "description": "CDK app for 7d2d infrastructure and deployment scaffold",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -w",
    "synth": "npx cdk synth",
    "deploy": "./bin/deploy.sh",
    "diff": "npx cdk diff",
    "destroy": "npx cdk destroy",
    "bootstrap": "npx cdk bootstrap"
  },
  "devDependencies": {
    "@types/node": "^20.14.12",
    "aws-cdk-lib": "^2.170.0",
    "constructs": "^10.4.2",
    "esbuild": "^0.28.1",
    "source-map-support": "^0.5.21",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
